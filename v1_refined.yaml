openapi: 3.0.0
info:
  title: Test API
  description: This is the old description of the API. It is very short.
  version: 1.0.0
paths:
  /test:
    get:
      summary: Old Summary
      description: Old description of the operation.
      responses:
        '200':
          description: Old response description.
          content:
            application/json:
              schema:
                type: object
                description: Old schema description.
                properties:
                  prop1:
                    type: string
                    description: Old property description.
