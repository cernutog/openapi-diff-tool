openapi: 3.0.0
info:
  title: Test API
  description: |
    Para 1: This is the first paragraph that will be modified.
    
    Para 2: This paragraph will be deleted.
    
    Para 3: This one too.
paths:
  /test:
    get:
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  qualifier:
                    type: string
                    description: "Qualifier: PVAH, PVHI, PVAG, EVOP"
